package com.xiaomi.smarthome.miio.camera.cloudstorage;

import android.content.Context;
import android.graphics.Canvas;
import android.util.AttributeSet;
import androidx.recyclerview.widget.RecyclerView;

public class CloudVideoDownloadListView extends RecyclerView {
    public CloudVideoDownloadListView(Context context) {
        super(context);
    }

    public CloudVideoDownloadListView(Context context, AttributeSet attributeSet) {
        super(context, attributeSet);
    }

    public CloudVideoDownloadListView(Context context, AttributeSet attributeSet, int i) {
        super(context, attributeSet, i);
    }

    public void onDraw(Canvas canvas) {
        super.onDraw(canvas);
    }

    public void onLayout(boolean z, int i, int i2, int i3, int i4) {
        super.onLayout(z, i, i2, i3, i4);
    }
}
