package _m_j;

import android.content.Context;

public interface azu {
    void processMessage(Context context, azx azx);

    void processMessage(Context context, azy azy);

    void processMessage(Context context, bab bab);
}
