package _m_j;

import java.util.Collections;
import java.util.List;

final class bcg {
    static <T> List<T> O000000o(List<T> list) {
        return list == null ? Collections.emptyList() : Collections.unmodifiableList(list);
    }
}
