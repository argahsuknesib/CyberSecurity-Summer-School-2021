package _m_j;

import android.content.Context;
import android.text.TextUtils;
import com.sankuai.waimai.router.annotation.RouterService;

@RouterService
public class htd implements ezj {
    public boolean dispatchToYouPin(String str) {
        return hjp.O000000o(str);
    }

    public boolean shopLauncherResolve(Context context, String str, boolean z) {
        if (context == null || TextUtils.isEmpty(str)) {
            return false;
        }
        hjp.O000000o(str);
        return true;
    }
}
