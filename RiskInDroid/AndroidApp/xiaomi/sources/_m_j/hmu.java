package _m_j;

import android.app.Activity;

public interface hmu {
    void checkPassedForCamera(Activity activity, boolean z, inc inc);

    void checkPassedForLocation(Activity activity, boolean z, inc inc, String str);
}
