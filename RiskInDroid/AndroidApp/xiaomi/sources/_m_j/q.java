package _m_j;

import android.graphics.Color;

public final class q {

    /* renamed from: O000000o  reason: collision with root package name */
    private static final ThreadLocal<double[]> f2391O000000o = new ThreadLocal<>();

    public static int O000000o(int i, int i2) {
        int alpha = Color.alpha(i2);
        int alpha2 = Color.alpha(i);
        int i3 = 255 - (((255 - alpha) * (255 - alpha2)) / 255);
        return Color.argb(i3, O000000o(Color.red(i), alpha2, Color.red(i2), alpha, i3), O000000o(Color.green(i), alpha2, Color.green(i2), alpha, i3), O000000o(Color.blue(i), alpha2, Color.blue(i2), alpha, i3));
    }

    private static int O000000o(int i, int i2, int i3, int i4, int i5) {
        if (i5 == 0) {
            return 0;
        }
        return (((i * 255) * i2) + ((i3 * i4) * (255 - i2))) / (i5 * 255);
    }

    public static int O00000Oo(int i, int i2) {
        if (i2 >= 0 && i2 <= 255) {
            return (i & 16777215) | (i2 << 24);
        }
        throw new IllegalArgumentException("alpha must be between 0 and 255.");
    }

    public static int O000000o(int i, int i2, float f) {
        float f2 = 1.0f - f;
        return Color.argb((int) ((((float) Color.alpha(i)) * f2) + (((float) Color.alpha(i2)) * f)), (int) ((((float) Color.red(i)) * f2) + (((float) Color.red(i2)) * f)), (int) ((((float) Color.green(i)) * f2) + (((float) Color.green(i2)) * f)), (int) ((((float) Color.blue(i)) * f2) + (((float) Color.blue(i2)) * f)));
    }
}
