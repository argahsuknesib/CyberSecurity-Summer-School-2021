package _m_j;

import android.renderscript.FieldPacker;
import android.renderscript.RSRuntimeException;
import android.renderscript.ScriptC;
import androidx.renderscript.Allocation;

final class ho extends ScriptC {
    /* access modifiers changed from: package-private */
    public final void O000000o(int i, Allocation allocation, Allocation allocation2, hl hlVar) {
        try {
            forEach(0, (android.renderscript.Allocation) null, allocation2 != null ? ((hh) allocation2).O0000o00 : null, (FieldPacker) null);
        } catch (RSRuntimeException e) {
            throw hk.O000000o(e);
        }
    }
}
